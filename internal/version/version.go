package version

// Name is the application name shown in the TUI header.
// Keep default to agtok; override only if rebranding.
var Name = "agtok"

// Version is injected at build time via:
//
// Defaults to "dev" when not injected.
var Version = "0.1.4"
